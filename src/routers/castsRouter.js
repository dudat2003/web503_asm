import Router from 'express';
// import { getAllCasts, deleteCast, addCastToMovie, updateCastInMovie, getCastByMovieId } from '../controllers/moviesController';

const router = Router();


// //Cast Router
// router.get("/", getAllCasts)
// router.get("/:id", getCastByMovieId)

// router.post("/:id", addCastToMovie)
// router.put('/:id/:castName', updateCastInMovie);
// router.delete('/:id', deleteCast)

export default router;